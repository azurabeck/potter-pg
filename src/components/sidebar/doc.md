# Sidebar

Barra vertical de ícones fixa à esquerda de toda a aplicação (fora do
`<Routes>`, ao lado do `Navbar`). Atalhos rápidos entre seções; o ícone
correspondente à rota atual recebe destaque em ouro.
