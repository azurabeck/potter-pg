# Plataforma

Página visual da mesa de RPG. O `index.tsx` coordena o estado da tela, `functions.ts` concentra tipos, constantes e helpers, `style.scss` mantém apenas os estilos da página e a pasta `components` contém os componentes exclusivos da Plataforma.
